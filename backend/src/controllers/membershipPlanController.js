const membershipPlanService=require('../services/membershipPlanService');
async function getPlans(req,res){try{res.json(await membershipPlanService.getPlans(req.user?.role==='admin'));}catch(e){console.error(e.message);res.status(500).json({error:'Failed to fetch membership plans'});}}
async function createPlan(req,res){try{const {name,planType='non_pro',description,price,durationDays,billingPeriod='yearly',monthlyBasePrice,discountPercent=0,benefits=[]}=req.body;if(!name||(price===undefined&&monthlyBasePrice===undefined))return res.status(400).json({error:'Name and price are required'});if(!['monthly','quarterly','yearly'].includes(billingPeriod))return res.status(400).json({error:'Invalid billing period'});res.status(201).json(await membershipPlanService.createPlan({name,planType,description,price,durationDays,billingPeriod,monthlyBasePrice,discountPercent,benefits}));}catch(e){console.error(e.message);res.status(500).json({error:e.code==='23505'?'A plan with this name already exists':'Failed to create membership plan'});}}
async function updatePlan(req,res){try{const p=await membershipPlanService.updatePlan(req.params.id,req.body);if(!p)return res.status(404).json({error:'Membership plan not found'});res.json(p);}catch(e){console.error(e.message);res.status(500).json({error:e.code==='23505'?'A plan with this name already exists':'Failed to update membership plan'});}}
async function setPlanStatus(req,res){try{const p=await membershipPlanService.setPlanStatus(req.params.id,Boolean(req.body.isActive));if(!p)return res.status(404).json({error:'Membership plan not found'});res.json(p);}catch(e){res.status(500).json({error:'Failed to update membership plan'});}}
async function deletePlan(req,res){try{const p=await membershipPlanService.deletePlan(req.params.id);if(!p)return res.status(404).json({error:'Membership plan not found'});res.json({message:'Membership plan deleted successfully'});}catch(e){res.status(500).json({error:'Failed to delete membership plan'});}}
module.exports={getPlans,createPlan,updatePlan,setPlanStatus,deletePlan};
