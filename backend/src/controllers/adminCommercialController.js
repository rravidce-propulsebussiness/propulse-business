const svc=require('../services/adminCommercialService');
const investmentSvc=require('../services/adminInvestmentService');
const payoutRequests=require('../services/investorPayoutRequestService');
async function investorSettings(req,res){try{res.json(await svc.getInvestorSettings())}catch(e){console.error(e);res.status(500).json({error:'Failed to load investor settings'})}}
async function updateInvestor(req,res){try{res.json(await svc.updateInvestorSettings(req.body))}catch(e){console.error(e);const status=['INVALID_LOCATION_CONFIG','INVALID_INDUSTRY_LOCATION_CONFIG','INVALID_INVESTMENT_CONFIG'].includes(e.code)?400:500;res.status(status).json({error:e.message||'Failed to save investor settings',code:e.code})}}
async function investmentDashboard(req,res){try{const dashboard=await investmentSvc.getDashboard({search:req.query.search,status:req.query.status,industryId:req.query.industryId});const investors=await Promise.all((dashboard?.investors||[]).map(async investor=>{try{const latestCycle=(await require('../config/database').query(`SELECT id FROM investment_cycles WHERE user_id=$1 ORDER BY CASE WHEN status IN ('ACTIVE','EXIT_REQUESTED','WAITING_FOR_LEADS') THEN 0 ELSE 1 END,id DESC LIMIT 1`,[Number(investor.user_id)])).rows[0];const funds=await payoutRequests.getInvestorFunds(Number(investor.user_id),latestCycle?.id||null);return {...investor,latest_cycle_id:latestCycle?.id?Number(latestCycle.id):null,ledger:{available_for_ads:Number(funds.available_for_ads||0),ad_spent:Number(funds.ad_spent||0),generated:Number(funds.generated||0),withdrawable_earnings:Number(funds.withdrawable_earnings||0),transferable:Number(funds.transferable||0),payout_reserved:Number(funds.payout_reserved||0),payout_transferred:Number(funds.payout_transferred||0),total_invested:Number(funds.total_invested||0)},payable_now:Number(funds.transferable||0)} }catch(e){console.error(`Investor ledger enrichment failed for ${investor.user_id}:`,e.message);return investor}}));res.json({...dashboard,investors})}catch(e){console.error(e);res.status(500).json({error:'Failed to load investor analytics'})}}
module.exports={investorSettings,updateInvestor,investmentDashboard};
