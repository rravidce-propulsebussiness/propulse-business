const pool = require('../config/database');
async function createManualPayment({userId,amount,manualReference,proofUrl,notes,membershipPlanId}){if(!manualReference||!String(manualReference).trim())throw Object.assign(new Error('Payment reference / UTR is required'),{code:'REFERENCE_REQUIRED'});if(!membershipPlanId)throw Object.assign(new Error('A Pro membership plan is required'),{code:'INVALID_PLAN'});const planResult=await pool.query(`SELECT id,name,plan_type,price,currency,billing_months,duration_days,is_active FROM membership_plans WHERE id=$1`,[membershipPlanId]);const plan=planResult.rows[0];if(!plan||!plan.is_active)throw Object.assign(new Error('Membership plan is not available'),{code:'PLAN_NOT_FOUND'});if(String(plan.plan_type).toLowerCase()!=='pro')throw Object.assign(new Error('Only the Pro membership can be purchased'),{code:'INVALID_PLAN'});const expected=Number(plan.price),submitted=Number(amount);if(!Number.isFinite(expected)||expected<=0||!Number.isFinite(submitted)||Math.abs(expected-submitted)>0.01)throw Object.assign(new Error('Payment amount does not match the selected Pro plan'),{code:'INVALID_AMOUNT'});const result=await pool.query(`INSERT INTO payments (user_id,membership_plan_id,amount,currency,method,status,reference_number,proof_url,notes) VALUES ($1,$2,$3,$4,'manual','pending',$5,$6,$7) RETURNING *`,[userId,plan.id,expected,plan.currency||'INR',String(manualReference).trim(),proofUrl||null,notes||`Pro membership: ${plan.name}`]);return result.rows[0]}
async function getPayments({status,search}){const values=[],where=[];if(status&&status!=='all'){values.push(status);where.push(`p.status=$${values.length}`)}if(search){values.push(`%${search}%`);where.push(`(u.name ILIKE $${values.length} OR u.email ILIKE $${values.length} OR COALESCE(p.reference_number,'') ILIKE $${values.length})`)}const result=await pool.query(`SELECT p.*,u.name AS user_name,u.email AS user_email,bp.business_name,mp.name AS membership_plan_name,mp.billing_period FROM payments p JOIN users u ON u.id=p.user_id LEFT JOIN business_profiles bp ON bp.user_id=u.id LEFT JOIN membership_plans mp ON mp.id=p.membership_plan_id ${where.length?`WHERE ${where.join(' AND ')}`:''} ORDER BY p.created_at DESC`,values);return result.rows}
async function activateMembership(client,payment){if(!payment.membership_plan_id)return null;const planResult=await client.query(`SELECT id,plan_type,billing_months,duration_days,is_active FROM membership_plans WHERE id=$1`,[payment.membership_plan_id]);const plan=planResult.rows[0];if(!plan||!plan.is_active)throw Object.assign(new Error('Membership plan is no longer available'),{code:'PLAN_NOT_FOUND'});if(String(plan.plan_type).toLowerCase()!=='pro')throw Object.assign(new Error('Only Pro membership payments can be activated'),{code:'INVALID_PLAN'});const currentResult=await client.query(`SELECT id,expires_at FROM memberships WHERE user_id=$1 AND status='active' AND expires_at>CURRENT_TIMESTAMP ORDER BY expires_at DESC LIMIT 1 FOR UPDATE`,[payment.user_id]);const current=currentResult.rows[0],now=new Date(),base=current&&new Date(current.expires_at)>now?new Date(current.expires_at):now,end=new Date(base);if(Number(plan.duration_days)>0)end.setDate(end.getDate()+Number(plan.duration_days));else end.setMonth(end.getMonth()+Math.max(1,Number(plan.billing_months||1)));if(current)await client.query(`UPDATE memberships SET membership_plan_id=$1,payment_id=$2,expires_at=$3,status='active',updated_at=CURRENT_TIMESTAMP WHERE id=$4`,[plan.id,payment.id,end,current.id]);else await client.query(`INSERT INTO memberships(user_id,membership_plan_id,payment_id,starts_at,expires_at,status) VALUES($1,$2,$3,$4,$5,'active')`,[payment.user_id,plan.id,payment.id,base,end]);return{planId:plan.id,expiresAt:end}}
async function updatePaymentStatus(id,status,adminId,notes){const client=await pool.connect();try{await client.query('BEGIN');const existing=await client.query(`SELECT * FROM payments WHERE id=$1 FOR UPDATE`,[id]);const payment=existing.rows[0];if(!payment){await client.query('ROLLBACK');return null}if(status==='paid'&&payment.method!=='manual')throw Object.assign(new Error('Only manual membership payments can be approved here'),{code:'PAYMENT_NOT_MANUAL'});let membership=null;if(status==='paid')membership=await activateMembership(client,payment);await client.query(`UPDATE payments SET status=$1,reviewed_by=$2,reviewed_at=CURRENT_TIMESTAMP,paid_at=CASE WHEN $1='paid' THEN CURRENT_TIMESTAMP ELSE paid_at END,notes=COALESCE($3,notes),updated_at=CURRENT_TIMESTAMP WHERE id=$4`,[status,adminId,notes||null,id]);const updated=(await client.query(`SELECT * FROM payments WHERE id=$1`,[id])).rows[0];await client.query('COMMIT');return{...updated,membership_activation:membership}}catch(error){await client.query('ROLLBACK');throw error}finally{client.release()}}
module.exports={createManualPayment,getPayments,updatePaymentStatus};