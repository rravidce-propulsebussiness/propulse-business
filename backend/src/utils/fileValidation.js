function decodeBase64Payload(value){const match=String(value||'').trim().match(/^data:([^;]+);base64,([A-Za-z0-9+/]+={0,2})$/i);if(!match)return null;const payload=match[2];const data=Buffer.from(payload,'base64');return{mime:match[1].toLowerCase(),data};}
function validateDataUrlSignature(value,allowed){const parsed=decodeBase64Payload(value);if(!parsed)return false;if(!allowed.includes(parsed.mime))return false;const {mime,data}=parsed;if(mime==='application/pdf')return data.subarray(0,5).toString('ascii')==='%PDF-';if(mime==='image/png')return data.subarray(0,8).equals(Buffer.from([137,80,78,71,13,10,26,10]));if(mime==='image/jpeg')return data.subarray(0,3).equals(Buffer.from([255,216,255]));if(mime==='image/webp')return data.subarray(0,4).toString('ascii')==='RIFF'&&data.subarray(8,12).toString('ascii')==='WEBP';return false;}
module.exports={decodeBase64Payload,validateDataUrlSignature};