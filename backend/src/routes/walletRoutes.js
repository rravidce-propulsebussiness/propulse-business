const express=require('express');const walletController=require('../controllers/walletController');const requireAuth=require('../middleware/authMiddleware');const requireAdmin=require('../middleware/adminMiddleware');const router=express.Router();
router.get('/',requireAuth,walletController.get);router.post('/topups',requireAuth,walletController.topup);router.get('/topups/history',requireAuth,walletController.myTopups);router.get('/topups',requireAdmin,walletController.listTopups);router.get('/admin/history/customers',requireAdmin,walletController.adminWalletCustomers);router.get('/admin/history/customers/:userId',requireAdmin,walletController.adminWalletCustomerDetails);router.post('/admin/history/customers/:userId/adjust',requireAdmin,walletController.adjustBalance);router.patch('/topups/:id/approve',requireAdmin,walletController.approve);router.patch('/topups/:id/reject',requireAdmin,walletController.reject);module.exports=router;
