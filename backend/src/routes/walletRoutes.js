const express=require('express');const walletController=require('../controllers/walletController');const requireAuth=require('../middleware/authMiddleware');const requireAdmin=require('../middleware/adminMiddleware');const router=express.Router();
router.get('/',requireAuth,walletController.get);router.post('/topups',requireAuth,walletController.topup);router.patch('/topups/:id/approve',requireAdmin,walletController.approve);router.patch('/topups/:id/reject',requireAdmin,walletController.reject);module.exports=router;
