import {useEffect,useRef,useState} from 'react';
import './AdminLeadsV6.css';
import {getToken} from '../../utils/auth';

const API='http://localhost:5000/api';
const TYPES=['basic','premium','exclusive'];
const txt=v=>String(v??'').trim();
const norm=v=>txt(v).toLowerCase().replace(/[^a-z0-9]/g,'');
const cap=v=>txt(v).replace(/[_-]/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
const emptyPricing={shares:[{shares:1,normal:null,pro:null},{shares:3,normal:null,pro:null},{shares:5,normal:null,pro:null}]};

function csvRows(text){const out=[];let row=[],cell='',q=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'){if(q&&text[i+1]==='"'){cell+='"';i++}else q=!q}else if(c===','&&!q){row.push(cell);cell=''}else if((c==='\n'||c==='\r')&&!q){if(c==='\r'&&text[i+1]==='\n')i++;row.push(cell);if(row.some(txt))out.push(row);row=[];cell=''}else cell+=c}row.push(cell);if(row.some(txt))out.push(row);if(!out.length)return[];const h=out[0].map(txt);return out.slice(1).map(r=>Object.fromEntries(h.map((x,i)=>[x,txt(r[i])] )))}
function val(raw,names){const k=Object.keys(raw).find(x=>names.some(n=>norm(x)===norm(n)));return k?txt(raw[k]):''}
function sheetPricing(raw){const pick=(tier,shares)=>{const names=[`${tier} ${shares} Share`,`${tier} ${shares} Shares`,`${tier} ${shares} Share Price`,`${tier} Price ${shares} Share`];const v=val(raw,names);return v===''?null:Number(v)};const shares=[1,3,5].map(n=>({shares:n,normal:pick('Normal',n),pro:pick('Pro',n)}));return shares.some(x=>x.normal!==null||x.pro!==null)?{shares}:null}
function downloadSample(){const h=['Lead Type','Exclusive Delay Hours','Industry','Service','Subservice','State','City','Customer Name','Customer Phone','Customer Email','Requirement','Property Type','Budget','Source','Notes','Project Type','Area / Size','Preferred Date','Normal 1 Share','Normal 3 Shares','Normal 5 Shares','Pro 1 Share','Pro 3 Shares','Pro 5 Shares'];const r=['premium','','Construction & Contractors','Building Construction','','Telangana','Hyderabad','Example Customer','9876500000','customer@example.com','Looking for a new house','Residential','2500000','Website','Example row','New Construction','2000 sq ft','2026-09-15','500','1200','1800','450','1050','1600'];const esc=v=>`"${String(v??'').replace(/"/g,'""')}"`;const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([h.map(esc).join(',')+'\n'+r.map(esc).join(',')],{type:'text/csv'}));a.download='propulse-leads-sample.csv';a.click();URL.revokeObjectURL(a.href)}

export default function AdminLeadsV6(){
 const [leads,setLeads]=useState([]),[cat,setCat]=useState({industries:[],services:[],subservices:[],states:[],cities:[]}),[rows,setRows]=useState([]),[selected,setSelected]=useState([]),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[error,setError]=useState(''),[upload,setUpload]=useState(false),[open,setOpen]=useState(null),[edit,setEdit]=useState(null);const file=useRef(null);
 const request=async(path,opt={})=>{const r=await fetch(API+path,{...opt,headers:{Authorization:`Bearer ${getToken()}`,'Content-Type':'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Request failed');return d};
 const load=async()=>{try{setLoading(true);const [l,i,s,ss,st,c]=await Promise.all([request('/leads?status=all'),request('/industries'),request('/services'),request('/subservices'),request('/states'),request('/cities')]);setLeads(l);setCat({industries:i,services:s,subservices:ss,states:st,cities:c})}catch(e){setError(e.message)}finally{setLoading(false)}};
 useEffect(()=>{load()},[]);
 const services=i=>cat.services.filter(x=>String(x.industry_id)===String(i));const subservices=s=>cat.subservices.filter(x=>String(x.service_id)===String(s));const cities=s=>cat.cities.filter(x=>String(x.state_id)===String(s));
 const find=(a,v)=>a.find(x=>norm(x.name)===norm(v));
 const make=(raw,n)=>{const i=find(cat.industries,val(raw,['Industry'])),s=i&&find(services(i.id),val(raw,['Service'])),ss=s&&find(subservices(s.id),val(raw,['Subservice'])),st=find(cat.states,val(raw,['State'])),c=st&&find(cities(st.id),val(raw,['City']));const pricing=sheetPricing(raw);const known=['leadtype','exclusivedelayhours','industry','service','subservice','state','city','customername','customerphone','customeremail','requirement','propertytype','budget','source','notes','projecttype','areasize','preferreddate','normal1share','normal3shares','normal5shares','pro1share','pro3shares','pro5shares'];const customFields={};Object.entries(raw).forEach(([k,v])=>{if(!known.includes(norm(k))&&txt(v))customFields[k]=v});return{tmp:`${Date.now()}-${n}-${Math.random()}`,leadType:(val(raw,['Lead Type'])||'basic').toLowerCase(),exclusiveDelayHours:val(raw,['Exclusive Delay Hours'])||24,industryId:i?.id||'',serviceId:s?.id||'',subserviceId:ss?.id||'',stateId:st?.id||'',cityId:c?.id||'',customerName:val(raw,['Customer Name']),customerPhone:val(raw,['Customer Phone']),customerEmail:val(raw,['Customer Email']),requirement:val(raw,['Requirement']),propertyType:val(raw,['Property Type']),budget:val(raw,['Budget']),source:val(raw,['Source'])||'upload',notes:val(raw,['Notes']),customFields,pricing:pricing||emptyPricing,pricingSource:pricing?'sheet':'rule',badI:!!val(raw,['Industry'])&&!i,badS:!!val(raw,['Service'])&&!s,badSS:!!val(raw,['Subservice'])&&!ss,badST:!!val(raw,['State'])&&!st,badC:!!val(raw,['City'])&&!c};};
 const uploadFile=async e=>{const f=e.target.files?.[0];if(!f)return;try{const raw=csvRows(await f.text());if(!raw.length)throw Error('Sheet is empty');const r=raw.map(make);setRows(r);setSelected(r.map(x=>x.tmp));setUpload(true)}catch(e2){setError(e2.message)}finally{e.target.value=''}};
 const change=(id,k,v)=>setRows(a=>a.map(r=>{if(r.tmp!==id)return r;const x={...r,[k]:v};if(k==='industryId'){x.serviceId='';x.subserviceId=''}if(k==='serviceId')x.subserviceId='';if(k==='stateId')x.cityId='';if(k==='leadType'&&v!=='exclusive')x.exclusiveDelayHours=0;return x}));
 const bulk=(k,v)=>setRows(a=>a.map(r=>selected.includes(r.tmp)?{...r,[k]:v,...(k==='industryId'?{serviceId:'',subserviceId:''}:{}),...(k==='serviceId'?{subserviceId:''}:{}),...(k==='stateId'?{cityId:''}:{}),...(k==='leadType'&&v!=='exclusive'?{exclusiveDelayHours:0}:{})}:r));
 const importRows=async()=>{setBusy(true);try{for(const r of rows.filter(x=>selected.includes(x.tmp))){const p={...r};delete p.tmp;['badI','badS','badSS','badST','badC'].forEach(k=>delete p[k]);await request('/leads',{method:'POST',body:JSON.stringify(p)})}setUpload(false);setRows([]);setSelected([]);await load()}catch(e){setError(e.message)}finally{setBusy(false)}};
 const remove=async id=>{if(!confirm('Delete this lead permanently?'))return;setBusy(true);try{await request(`/leads/${id}`,{method:'DELETE'});setOpen(null);await load()}catch(e){setError(e.message)}finally{setBusy(false)}};
 const beginEdit=l=>setEdit({...l,industryId:l.industry_id||'',serviceId:l.service_id||'',subserviceId:l.subservice_id||'',stateId:l.state_id||'',cityId:l.city_id||'',customerName:l.customer_name||'',customerPhone:l.customer_phone||'',customerEmail:l.customer_email||'',requirement:l.requirement||'',propertyType:l.property_type||'',budget:l.budget||'',source:l.source||'upload',status:l.status||'available',notes:l.notes||'',customFields:l.custom_fields||{},pricing:l.pricing||emptyPricing,leadType:l.lead_type||'basic',exclusiveDelayHours:l.exclusive_delay_hours??24});
 const save=async()=>{setBusy(true);try{await request(`/leads/${edit.id}`,{method:'PUT',body:JSON.stringify(edit)});setEdit(null);await load()}catch(e){setError(e.message)}finally{setBusy(false)}};
 const counts=type=>leads.filter(l=>l.lead_type===type).length;
 return <div className="v6-leads"><header className="v6-head"><div><span className="v6-kicker">LEAD MARKETPLACE</span><h1>Leads</h1><p>Upload, classify and manage your lead inventory.</p></div><div className="v6-actions"><button className="v6-btn ghost" onClick={downloadSample}>↓ Sample Sheet</button><button className="v6-btn primary" onClick={()=>file.current?.click()}>↑ Upload Leads</button><input ref={file} hidden type="file" accept=".csv" onChange={uploadFile}/></div></header>
 <div className="v6-type-tabs"><TypeStat type="basic" count={counts('basic')}/><TypeStat type="premium" count={counts('premium')}/><TypeStat type="exclusive" count={counts('exclusive')}/><TypeStat type="all" count={leads.length}/></div>
 {error&&<div className="v6-alert">{error}<button onClick={()=>setError('')}>×</button></div>}
 <section className="v6-panel"><div className="v6-panel-head"><div><h2>Lead inventory</h2><span>{leads.length} total leads · cards are expandable</span></div><button className="v6-btn ghost" onClick={load}>↻ Refresh</button></div>{loading?<div className="v6-empty">Loading leads…</div>:!leads.length?<div className="v6-empty">No leads yet. Upload your first CSV.</div>:<div className="v6-grid">{leads.map(l=><LeadCard key={l.id} lead={l} open={open===l.id} toggle={()=>setOpen(open===l.id?null:l.id)} edit={()=>beginEdit(l)} remove={()=>remove(l.id)}/>)}</div>}</section>
 {upload&&<Upload rows={rows} selected={selected} setSelected={setSelected} cat={cat} services={services} subservices={subservices} cities={cities} change={change} bulk={bulk} close={()=>setUpload(false)} save={importRows} busy={busy}/>} {edit&&<Editor data={edit} setData={setEdit} cat={cat} services={services} subservices={subservices} cities={cities} close={()=>setEdit(null)} save={save} busy={busy}/>} </div>;
}
function TypeStat({type,count}){return <div className={`v6-type ${type}`}><span>{type==='all'?'All Leads':cap(type)}</span><strong>{count}</strong></div>}
function Badge({type}){return <span className={`v6-badge ${type}`}>{cap(type)}</span>}
function LeadCard({lead,open,toggle,edit,remove}){return <article className={`v6-card ${lead.lead_type} ${open?'is-open':''}`}><div className="v6-card-top"><Badge type={lead.lead_type||'basic'}/><span className={`v6-status ${lead.status}`}>{lead.status}</span></div><button className="v6-card-main" onClick={toggle}><div className="v6-card-id">#{lead.id}</div><h3>{lead.customer_name||'Untitled Lead'}</h3><p className="v6-contact">{lead.customer_phone||'No phone'}{lead.customer_email?` · ${lead.customer_email}`:''}</p><div className="v6-meta"><div><small>Market</small><b>{lead.industry_name||'Unclassified'}</b><span>{lead.service_name||'No service'}{lead.subservice_name?` · ${lead.subservice_name}`:''}</span></div><div><small>Location</small><b>{lead.city_name||'—'}</b><span>{lead.state_name||'—'}</span></div></div><div className="v6-bottom"><strong>{lead.budget?`₹ ${lead.budget}`:'Budget not set'}</strong><span>{open?'Hide details':'View details'} {open?'⌃':'⌄'}</span></div></button><div className="v6-card-actions"><button onClick={edit}>✎ Edit</button><button className="danger" onClick={remove}>Delete</button></div>{open&&<div className="v6-expanded"><div className="v6-requirement"><small>Requirement</small><p>{lead.requirement||'No requirement provided.'}</p></div><div className="v6-detail-grid">{[['Property Type',lead.property_type],['Source',lead.source],['Phone',lead.customer_phone],['Email',lead.customer_email],['Industry',lead.industry_name],['Service',lead.service_name],['Subservice',lead.subservice_name],['Location',`${lead.city_name||'—'}, ${lead.state_name||'—'}`],['Lead Type',cap(lead.lead_type)],['Exclusive Delay',lead.lead_type==='exclusive'?`${lead.exclusive_delay_hours??24} hours`:'—']].map(([a,b])=><div key={a}><small>{a}</small><b>{b||'—'}</b></div>)}{Object.entries(lead.custom_fields||{}).map(([a,b])=><div key={a}><small>{cap(a)}</small><b>{String(b)}</b></div>)}</div>{lead.notes&&<div className="v6-notes"><small>Notes</small><p>{lead.notes}</p></div>}<div className="v6-footer"><span>Created {lead.created_at?new Date(lead.created_at).toLocaleString():'—'}</span><div><button onClick={edit}>Edit Lead</button><button className="danger" onClick={remove}>Delete Lead</button></div></div></div>}</article>}
function Select({label,value,options,onChange,disabled=false}){return <label className="v6-field"><span>{label}</span><select value={value||''} disabled={disabled} onChange={e=>onChange(e.target.value)}><option value="">None</option>{options.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>}
function Upload({rows,selected,setSelected,cat,services,subservices,cities,change,bulk,close,save,busy}){const all=rows.length>0&&selected.length===rows.length;const toggle=id=>setSelected(s=>s.includes(id)?s.filter(x=>x!==id):[...s,id]);return <div className="v6-overlay"><div className="v6-upload"><div className="v6-modal-head"><div><span className="v6-kicker">IMPORT</span><h2>Preview & Match</h2><p>{rows.length} rows · pricing is read from the sheet when supplied; otherwise the configured market rule is used.</p></div><button onClick={close}>×</button></div><div className="v6-bulk"><label><input type="checkbox" checked={all} onChange={e=>setSelected(e.target.checked?rows.map(r=>r.tmp):[])}/> Select all ({selected.length}/{rows.length})</label><Bulk label="Industry" value="" options={cat.industries} onChange={v=>bulk('industryId',v)}/><Bulk label="Service" value="" options={services(rows.find(r=>selected.includes(r.tmp))?.industryId)} onChange={v=>bulk('serviceId',v)}/><Bulk label="Subservice" value="" options={subservices(rows.find(r=>selected.includes(r.tmp))?.serviceId)} onChange={v=>bulk('subserviceId',v)}/><Bulk label="State" value="" options={cat.states} onChange={v=>bulk('stateId',v)}/><Bulk label="City" value="" options={cities(rows.find(r=>selected.includes(r.tmp))?.stateId)} onChange={v=>bulk('cityId',v)}/><Bulk label="Lead Type" value="" options={TYPES.map(x=>({id:x,name:cap(x)}))} onChange={v=>bulk('leadType',v)}/></div><div className="v6-preview">{rows.map(r=><div className={`v6-preview-row ${selected.includes(r.tmp)?'selected':''}`} key={r.tmp}><input type="checkbox" checked={selected.includes(r.tmp)} onChange={()=>toggle(r.tmp)}/><div className="v6-preview-fields"><Select label="Type" value={r.leadType} options={TYPES.map(x=>({id:x,name:cap(x)}))} onChange={v=>change(r.tmp,'leadType',v)}/><Select label="Industry" value={r.industryId} options={cat.industries} onChange={v=>change(r.tmp,'industryId',v)}/><Select label="Service" value={r.serviceId} options={services(r.industryId)} onChange={v=>change(r.tmp,'serviceId',v)} disabled={!r.industryId}/><Select label="Subservice" value={r.subserviceId} options={subservices(r.serviceId)} onChange={v=>change(r.tmp,'subserviceId',v)} disabled={!r.serviceId}/><Select label="State" value={r.stateId} options={cat.states} onChange={v=>change(r.tmp,'stateId',v)}/><Select label="City" value={r.cityId} options={cities(r.stateId)} onChange={v=>change(r.tmp,'cityId',v)} disabled={!r.stateId}/>{r.leadType==='exclusive'&&<label className="v6-field"><span>Pro-only delay (hours)</span><input type="number" min="0" max="8760" value={r.exclusiveDelayHours??24} onChange={e=>change(r.tmp,'exclusiveDelayHours',e.target.value)}/></label>}<div className="v6-preview-info"><b>{r.customerName||'Untitled lead'}</b><span>{r.customerPhone||'No phone'} · {r.requirement||'No requirement'}</span><em>{r.pricingSource==='sheet'?'Sheet pricing':'Market rule pricing'}</em></div></div></div>)}</div><div className="v6-modal-foot"><span>{selected.length} selected · all other uploaded fields remain dynamic</span><button className="v6-btn ghost" onClick={close}>Cancel</button><button className="v6-btn primary" disabled={!selected.length||busy} onClick={save}>{busy?'Importing…':'Import Leads'}</button></div></div></div>}
function Bulk({label,value,options,onChange}){return <select className="v6-bulk-select" value={value} onChange={e=>onChange(e.target.value)}><option value="">Bulk {label}</option>{options?.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select>}
function Editor({data,setData,cat,services,subservices,cities,close,save,busy}){const set=(k,v)=>setData(x=>({...x,[k]:v}));return <div className="v6-overlay"><div className="v6-editor"><div className="v6-modal-head"><div><span className="v6-kicker">LEAD MANAGEMENT</span><h2>Edit Lead #{data.id}</h2><p>All lead fields remain optional.</p></div><button onClick={close}>×</button></div><div className="v6-editor-grid"><Select label="Lead Type" value={data.leadType} options={TYPES.map(x=>({id:x,name:cap(x)}))} onChange={v=>{set('leadType',v);if(v!=='exclusive')set('exclusiveDelayHours',0)}}/><Select label="Industry" value={data.industryId} options={cat.industries} onChange={v=>{set('industryId',v);set('serviceId','');set('subserviceId','')}}/><Select label="Service" value={data.serviceId} options={services(data.industryId)} onChange={v=>{set('serviceId',v);set('subserviceId','')}}/><Select label="Subservice" value={data.subserviceId} options={subservices(data.serviceId)} onChange={v=>set('subserviceId',v)}/><Select label="State" value={data.stateId} options={cat.states} onChange={v=>{set('stateId',v);set('cityId','')}}/><Select label="City" value={data.cityId} options={cities(data.stateId)} onChange={v=>set('cityId',v)}/>{data.leadType==='exclusive'&&<label className="v6-field"><span>Pro-only delay (hours)</span><input type="number" min="0" max="8760" value={data.exclusiveDelayHours??24} onChange={e=>set('exclusiveDelayHours',e.target.value)}/></label>}{[['customerName','Customer Name'],['customerPhone','Phone'],['customerEmail','Email'],['propertyType','Property Type'],['budget','Budget'],['source','Source']].map(([k,l])=><label className="v6-field" key={k}><span>{l}</span><input value={data[k]||''} onChange={e=>set(k,e.target.value)}/></label>)}<label className="v6-field wide"><span>Requirement</span><textarea value={data.requirement||''} onChange={e=>set('requirement',e.target.value)}/></label><label className="v6-field wide"><span>Notes</span><textarea value={data.notes||''} onChange={e=>set('notes',e.target.value)}/></label><label className="v6-field"><span>Status</span><select value={data.status||'available'} onChange={e=>set('status',e.target.value)}>{['available','paused','sold','closed','invalid'].map(x=><option key={x}>{x}</option>)}</select></label></div><div className="v6-modal-foot"><span>Pricing is stored with the lead and is not configured here.</span><button className="v6-btn ghost" onClick={close}>Cancel</button><button className="v6-btn primary" disabled={busy} onClick={save}>{busy?'Saving…':'Save Changes'}</button></div></div></div>}
