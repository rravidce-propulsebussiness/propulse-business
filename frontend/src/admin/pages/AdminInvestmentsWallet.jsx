import { useEffect, useMemo, useState } from 'react'
import { apiRequest } from '../../utils/api'
import { getToken, clearSession } from '../../utils/auth'
import { useNavigate } from 'react-router-dom'
import './AdminInvestmentsPremium.css'

const money = value => `₹${Number(value || 0).toLocaleString('en-IN', { maximumFractionDigits: 2 })}`
const date = value => value ? new Date(value).toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' }) : '—'
const dateTime = value => value ? new Date(value).toLocaleString('en-IN', { day: '2-digit', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' }) : '—'
const MATURITY_OPTIONS = [
  { value: 'immediate', label: 'Immediate · testing' }, { value: '7', label: '7 days' }, { value: '14', label: '14 days' },
  { value: '30', label: '30 days' }, { value: '90', label: '90 days' }, { value: '180', label: '180 days' }, { value: '365', label: '365 days' }, { value: 'custom', label: 'Custom' },
]

const historyStyles = `
.investor-history-modal{width:min(980px,100%);padding:0;overflow:hidden;background:#fff;border:1px solid #dce7f2}.investor-history-head{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:22px 26px;border-bottom:1px solid #e4ebf3}.investor-history-head-left{display:flex;align-items:center;gap:13px;min-width:0}.investor-history-avatar{display:grid;place-items:center;width:44px;height:44px;border-radius:12px;background:#edf5fd;color:#165394;font-size:14px;font-weight:900;flex:0 0 44px}.investor-history-head h2{margin:0;color:#123d7b;font-size:20px;font-weight:850}.investor-history-head p{margin:4px 0 0;color:#7a8da5;font-size:10px}.investor-history-close{width:34px;height:34px;border:1px solid #dce6ef;border-radius:9px;background:#fff;color:#5c718b;font-size:20px;cursor:pointer}.investor-history-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;padding:18px 26px;background:#f8fbfe;border-bottom:1px solid #e5edf5}.history-summary-card{padding:14px 15px;border:1px solid #dce8f3;border-radius:10px;background:#fff}.history-summary-card span{display:block;color:#7287a2;font-size:9px;font-weight:850}.history-summary-card strong{display:block;margin-top:6px;color:#123f7b;font-size:19px;font-weight:850}.history-summary-card.green strong{color:#07985a}.history-summary-card.orange strong{color:#dd542f}.investor-history-body{padding:0 26px 22px}.history-caption{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:18px 0 12px}.history-caption h3{margin:0;color:#123d7b;font-size:14px;font-weight:850}.history-caption span{color:#7a8ea8;font-size:9px}.history-table{border:1px solid #dce7f1;border-radius:11px;overflow:hidden}.history-table-head,.history-table-row{display:grid;grid-template-columns:42px minmax(155px,1.3fr) 120px 120px minmax(210px,2fr) 145px;gap:10px;align-items:center}.history-table-head{min-height:42px;padding:0 14px;background:#f3f7fb;color:#7388a3;font-size:9px;font-weight:900}.history-table-row{min-height:67px;padding:8px 14px;border-top:1px solid #e8eef4}.history-index{color:#4c6786;font-size:9px}.history-type{display:flex;align-items:center;gap:9px;min-width:0}.history-icon{display:grid;place-items:center;width:29px;height:29px;border-radius:9px;font-size:15px;font-weight:900;flex:0 0 29px}.history-icon.investment{background:#eaf3ff;color:#1679df}.history-icon.spend{background:#fff0eb;color:#ed5b36}.history-icon.revenue{background:#e8faf1;color:#0a9b5c}.history-icon.reinvest{background:#e8f8f2;color:#08a16a}.history-icon.paid{background:#fff0eb;color:#dd542f}.history-type strong{display:block;color:#173f78;font-size:10px;font-weight:850;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.history-amount{font-size:11px;font-weight:900;white-space:nowrap}.history-amount.credit{color:#07985a}.history-amount.debit{color:#df5030}.history-balance{color:#153f78;font-size:11px;font-weight:850;white-space:nowrap}.history-description{color:#617894;font-size:9px;line-height:1.4}.history-description small{color:#91a0b2}.history-date{color:#496785;font-size:9px;line-height:1.4}.history-empty{padding:46px 20px;text-align:center;color:#8497ae;font-size:11px}.history-note{display:flex;align-items:flex-start;gap:9px;margin-top:14px;padding:12px 14px;border-radius:9px;background:#edf5fe;color:#5e7898;font-size:9px;line-height:1.5}.history-note i{display:grid;place-items:center;width:17px;height:17px;border:2px solid #1684eb;border-radius:50%;color:#1684eb;font-style:normal;font-size:9px;font-weight:900;flex:0 0 17px}@media(max-width:820px){.investor-history-summary{grid-template-columns:repeat(2,minmax(0,1fr))}.history-table{overflow:auto}.history-table-head,.history-table-row{min-width:800px}.investor-history-head,.investor-history-body{padding-left:16px;padding-right:16px}.investor-history-summary{padding-left:16px;padding-right:16px}}
`

export default function AdminInvestmentsWallet() {
  const navigate = useNavigate()
  const [data, setData] = useState({ investors: [] })
  const [search, setSearch] = useState(''); const [status, setStatus] = useState('all'); const [industryId, setIndustryId] = useState('')
  const [industries, setIndustries] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState('')
  const [account, setAccount] = useState(null); const [linked, setLinked] = useState(null); const [linkedLoading, setLinkedLoading] = useState(false)
  const [settings, setSettings] = useState(null); const [maturityPreset, setMaturityPreset] = useState('30'); const [maturityDays, setMaturityDays] = useState('30'); const [commissionPercent, setCommissionPercent] = useState('5')
  const [settingsLoading, setSettingsLoading] = useState(true); const [settingsBusy, setSettingsBusy] = useState(false); const [settingsMessage, setSettingsMessage] = useState(''); const [commissionMessage, setCommissionMessage] = useState('')

  const request = async (path, options = {}) => { if (!getToken()) { clearSession(); navigate('/login', { replace: true }); throw new Error('Your admin session has expired. Please sign in again.') } return apiRequest(path, options) }
  const load = async (silent = false) => { if (!silent) setLoading(true); setError(''); try { const params = new URLSearchParams({ search: search.trim(), status, industryId }); const [dashboard, industryData] = await Promise.all([request(`/admin/commercial/investment-dashboard?${params}`), industries.length ? Promise.resolve(industries) : request('/industries')]); setData({ investors: dashboard.investors || [] }); if (!industries.length) setIndustries(Array.isArray(industryData) ? industryData : industryData?.data || []) } catch (e) { if (!silent) setError(e.message || 'Unable to load investor accounts.') } finally { if (!silent) setLoading(false) } }
  const loadSettings = async () => { setSettingsLoading(true); try { const result = await request('/admin/commercial/investor-settings'); setSettings(result); const days = Number(result.investment_cycle_days ?? 30); setMaturityPreset(days === 0 ? 'immediate' : MATURITY_OPTIONS.some(x => x.value === String(days)) ? String(days) : 'custom'); setMaturityDays(String(days)); const share = Number(result.investor_revenue_share_percent); setCommissionPercent(String(Number.isFinite(share) ? Math.max(0, Math.min(100, 100 - share)) : 5)) } catch (e) { setError(e.message || 'Unable to load investment settings.') } finally { setSettingsLoading(false) } }
  useEffect(() => { load(); loadSettings() }, [status, industryId])

  const investors = useMemo(() => data.investors.map(item => { const records = Array.isArray(item.cycles) ? item.cycles.filter(x => x.status !== 'cancelled') : []; const contributed = records.filter(x => !x.is_reinvestment).reduce((s, x) => s + Number(x.amount || 0), 0); const totalFunding = records.reduce((s, x) => s + Number(x.amount || 0), 0); const adSpent = records.reduce((s, x) => s + Number(x.ad_spent || 0), 0); const gross = Number(item.linked_gross_sales || 0); const payable = Math.max(0, Number(item.payable_now || 0)); const statusRecord = records.find(x => x.status === 'active') || records.find(x => x.status === 'matured') || records.find(x => x.status === 'pending') || records[0]; const joinedAt = records.reduce((oldest, row) => !oldest || new Date(row.created_at) < new Date(oldest) ? row.created_at : oldest, null); return { ...item, records, contributed, adBalance: Math.max(0, totalFunding - adSpent), gross, payable, accountStatus: statusRecord?.status || 'active', joinedAt } }), [data.investors])
  const totals = useMemo(() => investors.reduce((o, x) => { o.capital += x.contributed; o.adBalance += x.adBalance; o.gross += x.gross; o.payable += x.payable; return o }, { capital: 0, adBalance: 0, gross: 0, payable: 0 }), [investors])

  const saveSettings = async (days, commission) => { if (!settings) return; const d = Number(days), c = Number(commission); if (!Number.isInteger(d) || d < 0 || d > 3650) throw new Error('Settlement period must be between 0 and 3650 days.'); if (!Number.isFinite(c) || c < 0 || c > 100) throw new Error('Commission must be between 0% and 100%.'); return request('/admin/commercial/investor-settings', { method: 'PUT', body: JSON.stringify({ globalLimit: settings.global_limit, defaultIndustryLimit: settings.default_industry_limit, customerIndustryLimit: settings.customer_industry_limit, minInvestment: settings.min_investment, maxInvestment: settings.max_investment == null ? '' : settings.max_investment, enabled: Boolean(settings.is_enabled ?? settings.enabled), requiresPro: Boolean(settings.requires_pro), investmentCycleDays: d, autoReinvest: settings.auto_reinvest == null ? false : Boolean(settings.auto_reinvest), investorRevenueSharePercent: 100 - c }) }) }
  const saveCommission = async () => { setSettingsBusy(true); setCommissionMessage(''); setError(''); try { const updated = await saveSettings(Number(settings?.investment_cycle_days ?? maturityDays ?? 30), commissionPercent); setSettings(updated); setCommissionMessage('Saved'); await load(true) } catch (e) { setError(e.message || 'Unable to save commission.') } finally { setSettingsBusy(false) } }
  const saveMaturity = async () => { setSettingsBusy(true); setSettingsMessage(''); setError(''); try { const updated = await saveSettings(maturityDays, commissionPercent); setSettings(updated); setSettingsMessage('Saved'); await load(true) } catch (e) { setError(e.message || 'Unable to save settlement period.') } finally { setSettingsBusy(false) } }

  const buildHistory = investor => {
    const records = [...investor.records].sort((a, b) => new Date(a.created_at) - new Date(b.created_at) || Number(a.id) - Number(b.id)); const childByParent = new Map(records.filter(x => x.parent_investment_id != null).map(x => [Number(x.parent_investment_id), x])); const events = []
    const push = (e, order) => events.push({ ...e, _order: order })
    records.forEach(record => { const funding = Number(record.amount || 0); const spend = Number(record.ad_spent || 0); const revenue = Number(record.allocated_revenue || 0); const ref = String(record.payout_transfer_reference || '').trim().toUpperCase(); const child = childByParent.get(Number(record.id));
      if (!record.is_reinvestment && funding > 0) push({ type: 'investment', date: record.created_at, title: 'Investment added', amount: funding, description: 'Investor contributed capital', reference: `#INV-${record.id}` }, 10)
      if (spend > 0) push({ type: 'spend', date: record.updated_at || record.created_at, title: 'Ads spent', amount: -spend, description: 'Actual advertising spend', reference: `#ADS-${record.id}` }, 20)
      if (revenue > 0) push({ type: 'revenue', date: record.updated_at || record.created_at, title: 'Lead sold', amount: revenue, description: `${Number(record.allocated_sales || record.linked_paid_sales || 0)} lead sale(s) purchased by a customer`, reference: `#SALE-${record.id}` }, 30)
      if (ref.startsWith('REINVESTMENT-') && child) { const amount = Number(child.amount || 0); if (amount > 0) push({ type: 'reinvest', date: child.created_at || record.updated_at || record.created_at, title: 'Reinvested', amount: -amount, description: 'Earnings moved back into advertising', reference: `#REINV-${child.id}` }, 40) }
      else if (record.status === 'paid' && Number(record.paid_to_investor || 0) > 0) { const amount = Number(record.paid_to_investor || 0); push({ type: 'paid', date: record.updated_at || record.created_at, title: 'Transferred to investor', amount: -amount, description: 'Earnings transferred to investor account', reference: `#TRF-${record.id}` }, 50) }
    })
    let balance = 0; return events.sort((a, b) => new Date(a.date) - new Date(b.date) || a._order - b._order).map((e, i) => { balance = Math.round((balance + e.amount + Number.EPSILON) * 100) / 100; return { ...e, balance, index: i + 1 } }).reverse()
  }
  const showAccount = investor => setAccount({ investor, flow: buildHistory(investor) })
  const showLinked = async investor => { setLinked({ investor, items: [] }); setLinkedLoading(true); try { const items = await request(`/investments/admin/investor/${investor.user_id}/linked-leads`); setLinked({ investor, items: Array.isArray(items) ? items : items?.data || [] }) } catch (e) { setError(e.message || 'Unable to load investor leads.'); setLinked(null) } finally { setLinkedLoading(false) } }

  return <main className="admin-investments-page"><style>{historyStyles}</style>
    <header className="admin-investments-head"><div className="admin-title-block"><div className="admin-breadcrumb"><span>Admin</span><b>/</b><strong>Investments</strong></div><h1>Investments</h1></div><div className="system-health"><i /> System healthy</div></header>
    {error && <div className="admin-investments-error">{error}</div>}
    <section className="admin-investment-stats"><article className="stat-capital"><span>Investor Capital</span><strong>{money(totals.capital)}</strong><small>Total investor-contributed capital</small></article><article className="stat-balance"><span>Ad Balance</span><strong>{money(totals.adBalance)}</strong><small>Capital and reinvested funds remaining for ads</small></article><article className="stat-revenue"><span>Lead Revenue</span><strong>{money(totals.gross)}</strong><small>Gross value of linked paid lead sales</small></article><article className="stat-transfer"><span>Ready to Transfer</span><strong>{money(totals.payable)}</strong><small>Matured investor earnings available</small></article></section>
    <section className="admin-settings-grid"><article className="settings-card"><div className="settings-card-head"><div className="settings-icon">%</div><div><h2>ProPulse Commission</h2><p>Set the platform commission retained from lead revenue.</p></div></div><div className="settings-bottom"><label className="settings-field"><span>Commission percentage</span><div className="percent-input"><input value={commissionPercent} onChange={e => setCommissionPercent(e.target.value)} type="number" min="0" max="100" step="0.1" disabled={settingsLoading}/><b>%</b></div></label><button onClick={saveCommission} disabled={settingsBusy || settingsLoading}>{settingsBusy ? 'Saving…' : 'Save'}</button></div>{commissionMessage && <div className="settings-info" style={{marginTop:10}}><i>✓</i><span>{commissionMessage}</span></div>}</article><article className="settings-card"><div className="settings-card-head"><div className="settings-icon clock">◷</div><div><h2>Investment Maturity</h2><p>Choose when investor earnings become ready for settlement.</p></div></div><div className="maturity-row"><label className="settings-field"><span>Maturity period</span><select value={maturityPreset} onChange={e => { setMaturityPreset(e.target.value); if (e.target.value !== 'custom') setMaturityDays(e.target.value === 'immediate' ? '0' : e.target.value) }} disabled={settingsLoading}>{MATURITY_OPTIONS.map(x => <option key={x.value} value={x.value}>{x.label}</option>)}</select></label>{maturityPreset === 'custom' && <label className="settings-field days-field"><span>Days</span><input value={maturityDays} onChange={e => setMaturityDays(e.target.value)} type="number" min="0" max="3650"/></label>}<button onClick={saveMaturity} disabled={settingsBusy || settingsLoading}>{settingsBusy ? 'Saving…' : 'Save'}</button></div>{settingsMessage && <div className="settings-info maturity-info"><i>✓</i><span>{settingsMessage}</span></div>}</article></section>
    <section className="admin-investor-list"><div className="admin-list-head"><div className="investors-title"><div className="investors-icon">↗</div><h2>Investor Accounts</h2></div><div className="admin-list-filters"><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search investor, email or industry"/><select value={status} onChange={e => setStatus(e.target.value)}><option value="all">All statuses</option><option value="active">Active</option><option value="matured">Matured</option><option value="paid">Paid</option><option value="pending">Pending</option></select><select value={industryId} onChange={e => setIndustryId(e.target.value)}><option value="">All industries</option>{industries.map(ind => <option key={ind.id} value={ind.id}>{ind.name}</option>)}</select><button onClick={() => load()}>Search</button></div></div><div className="investor-table-wrap"><div className="investor-table-head"><span>#</span><span>Investor</span><span>Capital</span><span>Ad Balance</span><span>Lead Revenue</span><span>Ready to Transfer</span><span>Status</span><span>Joined</span><span>Actions</span></div><div className="investor-table-body">{loading ? <div className="admin-empty">Loading investor accounts…</div> : investors.length === 0 ? <div className="admin-empty">No investor accounts found.</div> : investors.map((investor, index) => <div className="investor-table-row" key={investor.user_id}><span className="row-number">{index + 1}</span><div className="row-investor"><div className="row-avatar">{String(investor.user_name || 'I').slice(0,1).toUpperCase()}</div><div><strong>{investor.user_name || 'Investor'}</strong><small>{investor.user_email || '—'}</small></div></div><span className="row-money">{money(investor.contributed)}</span><span className="row-money">{money(investor.adBalance)}</span><span className="row-money">{money(investor.gross)}</span><span className="row-money">{money(investor.payable)}</span><span className={`status-pill ${investor.accountStatus}`}>{investor.accountStatus}</span><span className="joined-date">{date(investor.joinedAt)}</span><div className="row-actions"><button className="view-btn" onClick={() => showAccount(investor)}>View</button><button className="more-btn" onClick={() => showLinked(investor)} title="View linked leads">•••</button></div></div>)}</div></div></section>
    {account && <div className="admin-modal-backdrop" onMouseDown={e => e.target === e.currentTarget && setAccount(null)}><div className="admin-modal investor-history-modal"><div className="investor-history-head"><div className="investor-history-head-left"><div className="investor-history-avatar">{String(account.investor.user_name || 'I').slice(0,1).toUpperCase()}</div><div><h2>{account.investor.user_name || 'Investor'} · Transaction History</h2><p>{account.investor.user_email || 'Investor account'} · One running balance for all money movement</p></div></div><button className="investor-history-close" onClick={() => setAccount(null)}>×</button></div><div className="investor-history-summary"><div className="history-summary-card"><span>Current Balance</span><strong>{money(account.flow[0]?.balance ?? 0)}</strong></div><div className="history-summary-card"><span>Total Invested</span><strong>{money(account.investor.contributed)}</strong></div><div className="history-summary-card green"><span>Total Lead Revenue</span><strong>{money(account.investor.gross)}</strong></div><div className="history-summary-card orange"><span>Ready to Transfer</span><strong>{money(account.investor.payable)}</strong></div></div><div className="investor-history-body"><div className="history-caption"><h3>Transaction History</h3><span>One running balance across every transaction</span></div><div className="history-table"><div className="history-table-head"><span>#</span><span>Type</span><span>Amount</span><span>Balance After</span><span>Description</span><span>Date</span></div>{account.flow.length === 0 ? <div className="history-empty">No transactions recorded yet.</div> : account.flow.map(event => <div className="history-table-row" key={`${event.index}-${event.reference}`}><span className="history-index">{event.index}</span><div className="history-type"><span className={`history-icon ${event.type}`}>{event.type === 'investment' ? '+' : event.type === 'spend' || event.type === 'paid' || event.type === 'reinvest' ? '−' : '₹'}</span><strong>{event.title}</strong></div><span className={`history-amount ${event.amount >= 0 ? 'credit' : 'debit'}`}>{event.amount >= 0 ? '+' : '−'}{money(Math.abs(event.amount))}</span><span className="history-balance">{money(event.balance)}</span><span className="history-description">{event.description}<br/><small>{event.reference}</small></span><span className="history-date">{dateTime(event.date)}</span></div>)}</div><div className="history-note"><i>i</i><span><strong>One running balance:</strong> investment added and lead revenue increase the balance; ads spent, reinvestments and actual investor transfers decrease it. Reinvestment child investments are never added twice.</span></div></div></div></div>}
    {linked && <div className="admin-modal-backdrop" onMouseDown={e => e.target === e.currentTarget && setLinked(null)}><div className="admin-modal"><button className="modal-close" onClick={() => setLinked(null)}>×</button><h2>Linked leads</h2><p>{linked.investor.user_name || 'Investor'} · Leads assigned to this investor.</p>{linkedLoading ? <div className="admin-empty">Loading linked leads…</div> : <div className="lead-table"><div className="lead-table-head"><span>Lead</span><span>Industry</span><span>Status</span><span>Customer</span><span>Revenue</span></div>{linked.items.length ? linked.items.map(item => <div className="lead-table-row" key={item.id}><strong>#{item.id}</strong><span>{item.industry_name || '—'}</span><span>{item.status || '—'}</span><span>{item.customer_name || '—'}</span><span>{money(item.investor_revenue || item.allocated_revenue || 0)}</span></div>) : <div className="admin-empty">No linked leads found.</div>}</div>}</div></div>}
  </main>
}
